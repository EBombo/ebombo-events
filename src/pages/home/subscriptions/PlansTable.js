import React, { useEffect, useGlobal, useState, useRef } from "reactn";
import styled from "styled-components";

export const PlansTable = (props) => {
  return <TableContainer>Tabla</TableContainer>;
};

const TableContainer = styled.div`
  width: 100%;
`;
