import React from "reactn";
import styled from "styled-components";

export const EventContainer = (props) => {
  return <EventContainerStyled>hola</EventContainerStyled>;
};

const EventContainerStyled = styled.div``;
